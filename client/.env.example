# Only needed if the frontend and backend are deployed separately (e.g. client on Vercel,
# server on Railway). Leave unset for local dev and single-service deploys.
# VITE_API_URL=https://your-backend.up.railway.app
